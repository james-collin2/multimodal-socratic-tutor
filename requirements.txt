# ─────────────────────────────────────────────────────────────
#  socratOT — Python 3.11 | AWS EC2 Linux x86_64
#  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Build tools (must be first) ───────────────
setuptools>=70.0.0
wheel>=0.44.0
packaging>=24.0

# ── OpenAI (primary LLM + embeddings) ─────────
openai>=1.35.0
langchain-openai

# ── LangChain framework ────────────────────────
langchain
langchain-community
langchain-core

# ── Vector stores ─────────────────────────────
chromadb==0.5.15
faiss-cpu==1.8.0.post1

# ── HuggingFace (sentence-transformers embeddings + RAGAS deps) ─────────
sentence-transformers==3.1.1
transformers==4.44.2
datasets==3.0.1
huggingface-hub==0.25.1
tokenizers==0.19.1
accelerate==0.34.2

# ── Vision ────────────────────────────────────
Pillow==10.4.0

# ── Data processing ───────────────────────────
pypdf==4.3.1
beautifulsoup4==4.12.3
requests==2.32.3
httpx==0.27.2
pandas==2.2.3
numpy==1.26.4
jsonlines==4.0.0
lxml==5.3.0

# ── Persistence & config ──────────────────────
sqlalchemy==2.0.35
aiosqlite==0.20.0
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# ── Async ─────────────────────────────────────
anyio==4.6.0

# ── YAML ──────────────────────────────────────
PyYAML>=6.0.2

# ── Evaluation & benchmarking ─────────────────
ragas==0.1.21
rouge-score==0.1.2
bert-score==0.3.13
tiktoken==0.7.0

# ── Streamlit UI ──────────────────────────────
# 1.43+ required for st.chat_input(accept_file=...) and st.audio_input (mic)
streamlit==1.52.2
plotly==5.24.1
python-multipart==0.0.12
watchdog==4.0.2

# ── Speech (accessibility) ────────────────────
# openai-whisper → install separately: pip install --no-build-isolation openai-whisper

# ── Dev tooling ───────────────────────────────
ruff==0.6.9
mypy==1.11.2
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pre-commit==3.8.0
loguru==0.7.2
